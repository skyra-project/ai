import { ConnectFour, TicTacToe } from '../index.mjs';

test('ConnectFour is a class', () => {
	expect(typeof ConnectFour).toBe('function');
});

test('TicTacToe is a class', () => {
	expect(typeof TicTacToe).toBe('function');
});
