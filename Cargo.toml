[package]
edition = "2021"
name = "skyra-ai"
version = "1.2.0"
authors = ["Skyra Project <contact@skyra.pw>"]
description = "Lightning-fast AI for Skyra Project's games"

[lib]
crate-type = ["cdylib"]

# [[bench]]
# name = "bench"
# harness = false

[dependencies]
itertools = "0.11.0"
napi = { version = "2.13.2", default-features = false, features = ["napi8"] }
napi-derive = "2.13.0"

[build-dependencies]
napi-build = "2.0.1"

[dev-dependencies]
criterion = "0.5.1"
rand = "0.8.5"
lazy_static = "1.4.0"

[profile.release]
lto = true
strip = true
